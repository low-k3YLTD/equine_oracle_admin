# ============================================================================
# Equine Oracle Admin Dashboard - Environment Configuration
# ============================================================================
# Copy this file to .env.local and fill in the actual values for your environment
# This file documents all required environment variables for the application
# ============================================================================

# --- MANUS PLATFORM CONFIGURATION ---
# These are automatically injected by Manus platform in production
# For local development, you may need to set these manually

# Application ID from Manus Dashboard
# Used for OAuth authentication and API identification
VITE_APP_ID=your_app_id_here

# OAuth Server URL for Manus authentication
# This is the endpoint for OAuth token exchange and user info retrieval
# Format: https://oauth.manus.io (or your custom OAuth server)
OAUTH_SERVER_URL=https://oauth.manus.io

# JWT Secret for session token signing
# Used to sign and verify session tokens
# Should be a strong, random string (minimum 32 characters recommended)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# --- DATABASE CONFIGURATION ---
# Connection string for the MySQL database

# MySQL Database URL
# Format: mysql://username:password@host:port/database_name
# Example: mysql://root:password@localhost:3306/equine_oracle
DATABASE_URL=mysql://root:password@localhost:3306/equine_oracle

# --- OWNER/ADMIN CONFIGURATION ---
# The OpenID of the admin user (owner) of the application
# This is obtained from the OAuth server after user authentication
OWNER_OPEN_ID=admin_user_open_id_here

# --- MANUS FORGE API CONFIGURATION ---
# Credentials for accessing Manus Forge API (LLM and other services)

# Forge API URL
# The endpoint for Manus Forge API services
BUILT_IN_FORGE_API_URL=https://api.manus.io/forge

# Forge API Key
# Authentication key for Forge API access
BUILT_IN_FORGE_API_KEY=your_forge_api_key_here

# --- RACING API CONFIGURATION ---
# Credentials for The Racing API integration
# Used for fetching live race data and predictions

# Racing API Username
# Your username for The Racing API authentication
RACING_API_USERNAME=your_racing_api_username

# Racing API Password
# Your password for The Racing API authentication
RACING_API_PASSWORD=your_racing_api_password

# --- OPTIONAL: ANALYTICS CONFIGURATION ---
# For Umami analytics tracking (optional)

# Analytics Endpoint
# The URL of your Umami analytics instance
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com

# Analytics Website ID
# The website ID in your Umami instance
VITE_ANALYTICS_WEBSITE_ID=your_website_id

# --- NODE ENVIRONMENT ---
# Determines whether the app runs in development or production mode
# Values: development, production
NODE_ENV=development

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in the values for your local environment
#    - For Manus OAuth, you'll need to set up OAuth credentials in Manus Dashboard
#
# 2. MANUS PLATFORM (Production):
#    - These variables are automatically injected by Manus
#    - Configure them in Settings → Secrets in the Management UI
#    - Required variables: VITE_APP_ID, OAUTH_SERVER_URL, JWT_SECRET, DATABASE_URL
#
# 3. DATABASE SETUP:
#    - Ensure MySQL is running and accessible
#    - Create a database named 'equine_oracle' (or your custom name)
#    - Run migrations: pnpm db:push
#
# 4. RACING API:
#    - Sign up at https://www.theracingapi.com
#    - Get your credentials from the API dashboard
#    - Add them to this configuration
#
# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# - NEVER commit .env.local or actual secrets to version control
# - Use strong, random values for JWT_SECRET and API keys
# - Rotate secrets regularly in production
# - Use a secrets manager (e.g., AWS Secrets Manager) in production
# - Ensure .env.local is in .gitignore (it should be by default)
#
# ============================================================================
